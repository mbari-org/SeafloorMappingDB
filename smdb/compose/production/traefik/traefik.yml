log:
  level: INFO

entryPoints:
  web:
    # http
    address: ":80"
    http:
      # https://docs.traefik.io/routing/entrypoints/#entrypoint
      redirections:
        entryPoint:
          to: web-secure

  web-secure:
    # https
    address: ":443"

http:
  routers:
    web-secure-router:
      rule: "Host(`smdb.shore.mbari.org`)"
      entryPoints:
        - web-secure
      middlewares:
        - csrf
      service: django

  middlewares:
    csrf:
      # https://docs.traefik.io/master/middlewares/headers/#hostsproxyheaders
      # https://docs.djangoproject.com/en/dev/ref/csrf/#ajax
      headers:
        hostsProxyHeaders: ["X-CSRFToken"]

  services:
    django:
      loadBalancer:
        servers:
          - url: http://django:5000

tls:
  certificates:
    # https://doc.traefik.io/traefik/master/https/tls/
    # Helpful example: https://gist.github.com/BretFisher/ede260ba033fabb3993062ae6e766f58
    - certFile: /certs/smdb.crt
      keyFile: /certs/smdb.key
  routers:
providers:
  # https://docs.traefik.io/master/providers/file/
  file:
    filename: /etc/traefik/traefik.yml
    watch: true
